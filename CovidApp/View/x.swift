

import Foundation
import Charts
import UIKit
import SwiftUI

class RecoveredPatients:ChartViewDelegate{
    
        let newView = UIHostingController(rootView: Board())
    func navigationController;?.pushViewController(newView, animated: true)
}
