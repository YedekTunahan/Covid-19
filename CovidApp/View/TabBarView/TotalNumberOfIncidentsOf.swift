//
//  TotalNumberOfIncidentsOf.swift
//  CovidApp
//
//  Created by Tunahan on 12/7/22.
//
import Charts
import UIKit

class TotalNumberOfIncidentsOf: UIViewController {

   
    

    @IBOutlet weak var label: UILabel!
 
    
    override func viewDidLoad() {
        super.viewDidLoad()

       
        
    }
    
 
    

    
    
    
    
    
    
    
    
    
    
    
    
    
    
    /*
    BAR CHART
     
     barChart.frame = CGRect(x: 0, y: 0, width: self.view.frame.size.width, height: self.view.frame.size.height)
     
     barChart.center = view.center
     view.addSubview(barChart)
     
     //MARK: VERİLERİ EKLEME KISMI GÜZEL KOD OLMASI ICIN KOD UZADI ENTRİES ARRAYI OLUSTURDUK
     var entries = [BarChartDataEntry]()
     for m in 0..<10 {
         entries.append(BarChartDataEntry(x: Double(m), y: Double(m)))
     }
     
     let set = BarChartDataSet(entries:
     entries)
     
     set.colors = ChartColorTemplates.joyful() // renklı grafık olması ıcın
     let data = BarChartData(dataSet: set)
     
     barChart.data = data
     
     
    */
    
    
    /*
     
     Lİne CHART
     
     
     
     lineChart.frame = CGRect(x: 0, y: 0, width: self.view.frame.size.width, height: self.view.frame.size.height)
     
     lineChart.center = view.center
     view.addSubview(lineChart)
     
     //MARK: VERİLERİ EKLEME KISMI GÜZEL KOD OLMASI ICIN KOD UZADI ENTRİES ARRAYI OLUSTURDUK
     var entries = [ChartDataEntry]()
     for m in 0..<10 {
         entries.append(ChartDataEntry(x: Double(m), y: Double(m)))
     }
     
     let set = LineChartDataSet(entries:
     entries)
     
     set.colors = ChartColorTemplates.joyful() // renklı grafık olması ıcın
     let data = LineChartData(dataSet: set)
     
     lineChart.data = data
     */

}
